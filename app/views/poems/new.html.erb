<h1>Write a poem</h1>

 <%= form_for(@poem) do |f| %>
     
     <%= f.label :content %>
     <%= f.text_field :content %>
     <%= f.submit "Submit Poem", :class => "btn btn-large btn-primary" %>   
    <% end %>